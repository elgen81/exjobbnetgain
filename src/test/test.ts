import assert = require('assert');
import fs = require('fs');




describe('File.ts', function() {
  describe('filetostring()', function() {
    it('should return the content of whitelist.ini', function() {
      
      assert.equal([1,2,3].indexOf(4), -1);
    });
  });
});

